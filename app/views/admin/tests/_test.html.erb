<tr>
  <td><%= test.title %></td>
  <td><%= test_level(test) %></td>
  <td><%= test.questions.size %></td>

  <td><%= link_to t('.show'), admin_test_path(test) %></td>      <!-- Просмотреть -->
  <td><%= link_to t('.edit'), edit_admin_test_path(test) %></td> <!-- Редактировать --> 
  <td><%= link_to t('.delete'), admin_test_path(test),              
          method: :delete, 
          data: { confirm: t('.choice') } %>                    <!-- Вы уверены? -->
  </td>
</tr>
